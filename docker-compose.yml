version: "3.8"

services:
  web:
    build: .
    container_name: uk-bonn-survey-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_BASE_URL: "https://survey-api.herz-check-bonn.de/api"
    # Uncomment and adjust if you also run the API via Compose
    # depends_on:
    #   - api

  # api:
  #   image: your-registry/uk-bonn-survey-api:latest
  #   container_name: uk-bonn-survey-api
  #   restart: always
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     NODE_ENV: production
  #     # DB_URL, etc.

